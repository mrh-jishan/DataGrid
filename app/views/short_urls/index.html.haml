%h4
  = @dashboard

%hr

%table.table
  %thead
    %tr
      %th #
      %th= t('.slug')
      %th= t('.expires_at')
      %th= t('.created_at')
      %th= t('.action')
  %tbody{id: "data_stream_files"}
    = render partial: "short_url", collection: @short_urls

