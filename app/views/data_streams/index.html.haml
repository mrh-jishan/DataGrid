= link_to t('.add_data_stream'),  new_data_stream_path, class: "btn btn-primary"

%hr

%table.table
  %thead
    %tr
      %th #
      %th Label
      %th CreatedAt
      %th Action
  %tbody{id: "data_streams"}
    = render partial: "data_streams/data_stream", collection: @data_streams
