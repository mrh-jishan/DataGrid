= link_to t('.setup_data_stream'), new_data_stream_path, class: "btn btn-sm btn-primary", data: { turbo_frame: "modal" }

.modal.fade{"aria-hidden" => "true", role: "dialog", "data-bs-focus": "false", "data-action" => "turbo:frame-load->modal#open turbo:submit-end->modal#close", "data-controller" => "modal"}
  .modal-dialog.modal-dialog-centered
    .modal-content
      = turbo_frame_tag "modal"

%hr

%table.table
  %thead
    %tr
      %th #
      %th Label
      %th Unique by
      -# %th Gid
      %th CreatedAt
      %th Action
  %tbody{id: "data_streams"}
    = render partial: "data_stream", collection: @data_streams
